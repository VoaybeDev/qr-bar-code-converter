import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.voaybedev.qrcodestudio',
  appName: 'QR Code Studio',
  webDir: 'build'
};

export default config;
